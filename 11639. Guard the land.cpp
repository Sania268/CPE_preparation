#include<iostream>
#include<algorithm>
#include<cassert>

void (int v) {
  assert(v >= 0);
  assert (v <= 100);
}

int main () {

  int T;
  cin >> T;
  int cas = 1;
  assert(T <= 100);
  while (T -- ) {
    
  }
  
  return 0;
}
